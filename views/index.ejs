<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Todo List</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <%- include('partials/header') %>

  <div class="container">
    <form method="get" action="/" id="filterForm">
      <label for="filter">Filter by Priority:</label>
      <select name="filter" id="filter" onchange="this.form.submit()">
        <option <%= filter === 'All' ? 'selected' : '' %>>All</option>
        <option <%= filter === 'High' ? 'selected' : '' %>>High</option>
        <option <%= filter === 'Medium' ? 'selected' : '' %>>Medium</option>
        <option <%= filter === 'Low' ? 'selected' : '' %>>Low</option>
      </select>
    </form>

    <form action="<%= editTodo ? '/todos/' + editTodo.id + '?_method=PUT' : '/todos' %>" method="post" id="todoForm">
      <input type="text" name="task" placeholder="Enter your todo" value="<%= editTodo ? editTodo.task : '' %>" required />
      <select name="priority" required>
        <option value="High" <%= editTodo && editTodo.priority === 'High' ? 'selected' : '' %>>High</option>
        <option value="Medium" <%= editTodo && editTodo.priority === 'Medium' ? 'selected' : '' %>>Medium</option>
        <option value="Low" <%= editTodo && editTodo.priority === 'Low' ? 'selected' : '' %>>Low</option>
      </select>
      <button type="submit"><%= editTodo ? 'Update' : 'Add' %></button>
      <% if (editTodo) { %>
        <a class="cancel-btn" href="/">Cancel</a>
      <% } %>
    </form>

    <ul class="todo-list">
      <% if (todos.length === 0) { %>
        <li>No todos found.</li>
      <% } else { %>
        <% todos.forEach(todo => { %>
          <li>
            <span><%= todo.task %> (<%= todo.priority %>)</span>
            <a href="/todos/<%= todo.id %>/edit">Edit</a>
            <form action="/todos/<%= todo.id %>?_method=DELETE" method="post" style="display:inline" onsubmit="return confirm('Delete this todo?')">
              <button type="submit">Delete</button>
            </form>
          </li>
        <% }) %>
      <% } %>
    </ul>
  </div>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
